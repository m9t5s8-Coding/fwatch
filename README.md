# fwatch

A simple command-line tool to watch and track file changes using hashing.

## Build

```bash
mkdir build && cd build
cmake ..
cmake --build .
```

## Usage

```bash
./fwatch [command]
```

## Status

Work in progress 🚧
